@model List<dttlLesson04.Models.DttlCustomer>
    @{
        ViewBag.Title = "danh sach khach hang";
        //var list = ViewBag.list;
        //var list = Model as List<dttlLesson04.Models.DttlCustomer>;
        int stt = 0;
    }

    <h2>danh sach khach hang</h2>
    <div class="row">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Ma so</th>
                    <th>ho va ten</th>
                    <th>dia chi</th>
                    <th>nam sinh</th>
                    <th> chuc nang</th>
                </tr>
            </thead>
            <tbody>
                @*foreach (var item in list)*@
                    @foreach (var item in Model)
                    {
                        stt++;
                        <tr>
                            <td>@stt</td>
                            <td>@item.CustomerId</td>
                            <td>@item.FirstName@item.@item.FirstName</td>
                            <td>@item.Address</td>
                            <td>@item.YearOfBirth</td>
                            <td>
                                <a href="#">Edit</a>
                                <a href="#">Deletw</a>

                            </td>
                        </tr>
                    }
            </tbody>

        </table>

    </div>

